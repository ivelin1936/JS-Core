;const validator = (function () {

    function isRegFormValid(data) {
        //TODO

        return true;
    }

    function isAddFormValid(data) {
        //TODO...

        return true;
    }

    return {
        isRegFormValid,
        isAddFormValid
    }
})();